# ASKHUMAN

- [ ] 2026-03-27 請在 GitHub 刪除 `Logos50607/dotfiles` 與 `Logos50607/digital-nomad` 兩個 remote repo（`personal/dotfiles` 與 `personal/digital-nomad` 本機也可一併清除）
  - `personal/dotfiles`：與 `internal-control/dotfiles` 完全重複
  - `personal/digital-nomad`：空目錄

- [ ] 2026-03-27 請封存 `Logos50607/TSL_project`（GitHub 可設為 archived）；`personal/TSL_project_backend` 與 `personal/TSL_project_frontend` 留在本機但不再發展

- [ ] 2026-03-27 請核可以下 personal repo 模組實際遷移計畫（文件已建立，等確認後執行搬碼）：
  - `thai-material` → `creation/thai-slides/` + `creation/thai-romanizer/`（pptx_generator + thai_processor）
  - `thai-trip-2026` → `intelligence/social-media-scraper/`（scraper/sources/ + url_pipeline/）
  - `build-work-station` → `intelligence/price-comparison/`（scraper/*.py）+ `infrastructure/workstation-spec/`（規格評估）
  - `knowledge-base` → `intelligence/ai-chat-crawler/`（crawlers/ + parse/）+ `education/knowledge-synthesis/`（summarize/）

- [ ] 2026-03-26 請核可 REPO_CLASSIFICATION.md 中的 repo 分組結果
