# Reports

執行報告歸檔目錄。由 `run-scheduled-task` 與 `daily-dispatch` workflow 自動產出。

## 目錄結構

```
reports/
├── YYYYMMDD/
│   ├── <task_name>.md        # 單一任務執行報告
│   └── daily-summary.md      # 每日彙整日報（聯絡組回報統整的輸入源）
```
