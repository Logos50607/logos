[project]
name = "digest"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "asyncpg",
    "python-dotenv",
    "psycopg2-binary>=2.9.11",
    "requests>=2.33.1",
]
